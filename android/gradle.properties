# JVM heap — Android Studio 默认 1.5G, Gradle 8 + Kotlin 2 + Compose 建议 4G
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:+HeapDumpOnOutOfMemoryError

# AndroidX 是默认; Jetifier 关掉 (老 support 库已经过保质期)
android.useAndroidX=true
android.enableJetifier=false

# Kotlin 编译器 incremental 默认开 — 改本地不要关; 关闭只在 CI 复现 bug 时
kotlin.code.style=official

# 子模块并行构建 + 缓存
org.gradle.parallel=true
org.gradle.caching=true

# 非 fatal 警告: 让 Gradle 一次性显示所有 deprecation, 不要 fail
org.gradle.warning.mode=summary
android.defaults.buildfeatures.resvalues=true
android.sdk.defaultTargetSdkToCompileSdkIfUnset=false
android.enableAppCompileTimeRClass=false
android.usesSdkInManifest.disallowed=false
android.uniquePackageNames=false
android.dependency.useConstraints=true
android.r8.strictFullModeForKeepRules=false
android.r8.optimizedResourceShrinking=false
android.builtInKotlin=false
android.newDsl=false
